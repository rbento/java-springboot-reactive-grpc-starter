dependencyResolutionManagement {
  versionCatalogs {
    create('libs') {
      from(files('../gradle/libs.versions.toml'))
    }
  }
}

rootProject.name = 'java-springboot-reactive-grpc-starter-build-logic'
